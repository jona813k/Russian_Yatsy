fastapi==0.135.2
uvicorn[standard]==0.42.0
pydantic==2.12.5
numpy==2.4.0
# torch is only needed locally for AI hints (model not deployed to Railway)
# Install manually with: pip install torch --index-url https://download.pytorch.org/whl/cpu
